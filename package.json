{
  "name": "mckennapsean.github.io",
  "version": "0.9.0",
  "scripts": {
    "install": "npm run build && npm run sync",
    "build": "jekeyll build",
    "start": "jekyll serve",
    "test": "npm run clean && npm run build && hs _site/",
    "sync": "git submodule update --init --recursive",
    "clean": "rm -rf _site/"
  },
  "devDependencies": {
    "font-awesome": "^4.6.3",
    "html5shiv": "^3.7.3",
    "jquery": "^2.1.1",
    "source-sans-pro": "^2.0.10",
    "tablesorter": "^2.27.1"
  }
}
